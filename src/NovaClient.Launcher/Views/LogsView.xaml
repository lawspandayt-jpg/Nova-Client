<UserControl x:Class="NovaClient.Launcher.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24,8,24,24" Style="{StaticResource PageTransition}" MaxWidth="1000">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Logs" FontSize="20" FontFamily="Segoe UI Semibold"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource GhostButton}" Content="Open Logs Folder" Padding="12,7" FontSize="12"
                        Command="{Binding OpenLogsFolderCommand}"/>
                <Button Style="{StaticResource GhostButton}" Content="Crash Reports" Padding="12,7" FontSize="12"
                        Margin="8,0,0,0" Command="{Binding OpenCrashReportsCommand}"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="14">
            <ScrollViewer VerticalScrollBarVisibility="Auto" x:Name="Scroller">
                <ItemsControl ItemsSource="{Binding Activity}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="11" FontFamily="Consolas"
                                       Foreground="{DynamicResource TextDimBrush}" Margin="0,1" TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
