<Window x:Class="NovaClient.Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:NovaClient.Launcher.ViewModels"
        xmlns:views="clr-namespace:NovaClient.Launcher.Views"
        Title="{Binding LauncherTitle}"
        Icon="Assets/logo.png"
        Width="980" Height="640" MinWidth="820" MinHeight="560"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource BgBrush}">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <views:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:HomeViewModel}">
            <views:HomeView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:OptiFineViewModel}">
            <views:OptiFineView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="44"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Custom title bar (double-click maximizes) -->
        <Grid Grid.Row="0" Grid.ColumnSpan="2" Background="Transparent"
              MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <StackPanel Orientation="Horizontal" Margin="16,0" VerticalAlignment="Center">
                <Path Width="18" Height="18" Stretch="Uniform" Fill="{DynamicResource AccentBrush}"
                      Data="M 50,0 L 62,38 100,50 62,62 50,100 38,62 0,50 38,38 Z"/>
                <TextBlock Text="{Binding LauncherTitle}" Margin="10,0,0,0" VerticalAlignment="Center"
                           FontFamily="Segoe UI Semibold" FontSize="14"/>
                <TextBlock Text="{Binding VersionText}" Margin="8,1,0,0" VerticalAlignment="Center"
                           FontSize="11" Foreground="{DynamicResource TextDimBrush}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0">
                <Button Style="{StaticResource CaptionButton}" Click="Minimize_Click" ToolTip="Minimize">
                    <Path Data="{StaticResource IconMinimize}" StrokeThickness="1.6" Stretch="Uniform" Width="11" Height="11"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                </Button>
                <Button x:Name="MaximizeButton" Style="{StaticResource CaptionButton}" Click="Maximize_Click" ToolTip="Maximize">
                    <Path x:Name="MaximizeIcon" Data="{StaticResource IconMaximize}" StrokeThickness="1.6"
                          Stretch="Uniform" Width="10" Height="10"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                </Button>
                <Button Style="{StaticResource CaptionButton}" Click="Close_Click" ToolTip="Close">
                    <Path Data="{StaticResource IconClose}" StrokeThickness="1.6" Stretch="Uniform" Width="10" Height="10"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Sidebar tabs -->
        <Border Grid.Row="1" Grid.Column="0" Background="{DynamicResource PanelBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
            <Grid>
                <StackPanel HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Style="{StaticResource SidebarButton}" ToolTip="Play" Command="{Binding NavPlayCommand}">
                        <Path Data="{StaticResource IconPlay}" Stretch="Uniform" Width="15" Height="15"
                              Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                    </Button>
                    <Button Style="{StaticResource SidebarButton}" ToolTip="OptiFine Setup" Command="{Binding NavOptiFineCommand}">
                        <Path Data="{StaticResource IconCube}" Stretch="Uniform" Width="16" Height="16" StrokeThickness="1.6"
                              StrokeLineJoin="Round"
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                    </Button>
                    <Button Style="{StaticResource SidebarButton}" ToolTip="Game Folder" Command="{Binding OpenFolderCommand}">
                        <Path Data="{StaticResource IconFolder}" Stretch="Uniform" Width="16" Height="14" StrokeThickness="1.6"
                              StrokeLineJoin="Round"
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                    </Button>
                    <Button Style="{StaticResource SidebarButton}" ToolTip="Website" Command="{Binding OpenWebsiteCommand}">
                        <Path Data="{StaticResource IconGlobe}" Stretch="Uniform" Width="16" Height="16" StrokeThickness="1.4"
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                    </Button>
                </StackPanel>
                <Button Style="{StaticResource SidebarButton}" ToolTip="Settings" Command="{Binding NavSettingsCommand}"
                        VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <Path Data="{StaticResource IconGear}" Stretch="Uniform" Width="15" Height="15" StrokeThickness="1.6"
                          StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                </Button>
            </Grid>
        </Border>

        <!-- Active screen -->
        <ContentControl Grid.Row="1" Grid.Column="1" Content="{Binding Current}"/>
    </Grid>
</Window>
